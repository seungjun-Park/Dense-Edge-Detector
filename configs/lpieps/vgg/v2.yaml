module:
  target: models.lpieps_v2.LPIEPSV2
  params:
    net_type: 'vgg'
    num_blocks: 2
    moderator_requires_grad: True
    lr: 1e-4
    weight_decay: 0.0
    lr_decay_epoch: 100
    log_interval: 50
    loss_config:
      target: losses.triplet.TripletMarginLoss
      params:
        margin: 0.5

data:
  target: datasets.util.DataModuleFromConfig
  params:
    num_workers: 4
    batch_size: 1
    wrap: True
    train:
      target: datasets.anime_triplet.AnimeTripletDataset
      params:
        root: '/local_datasets/anime'
        # root: 'D:/datasets/anime'
        train: True
        size: [512, 512]
        scale: [ 0.75, 1.0 ]

    validation:
      target: datasets.anime_triplet.AnimeTripletDataset
      params:
        root: '/local_datasets/anime'
        # root: 'D:/datasets/anime'
        train: False
        size: [512, 512]
        scale: [ 0.75, 1.0 ]

logger:
  target: pytorch_lightning.loggers.tensorboard.TensorBoardLogger
  params:
    save_dir: './logs'
    name: 'lpieps/vgg'
    version: 'v2'


checkpoints:
  latest_checkpoints:
    target: pytorch_lightning.callbacks.ModelCheckpoint
    params:
      dirpath: './checkpoints/lpieps/vgg/v2'
      filename: 'last'
      monitor: 'step'
      mode: 'max'
      save_top_k: 1

  best_checkpoint:
    target: pytorch_lightning.callbacks.ModelCheckpoint
    params:
      dirpath: './checkpoints/lpieps/vgg/v2'
      filename: 'best'
      monitor: 'valid/loss'
      mode: 'min'
      save_top_k: 1

trainer:
  accelerator: 'gpu'
  max_epochs: 100
  accumulate_grad_batches: 5